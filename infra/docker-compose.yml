# Backward compatibility: delegate to the canonical compose file at the repo root.
# From repo root, prefer: `docker compose up --build`
#
# From this directory: `docker compose -f docker-compose.yml up --build`

include:
  - path: ../docker-compose.yml
